#include <iostream>

using namespace std;

// A clerk at a shipping company is charged with the task of rearranging a number of
// large crates in order of the time they are to be shipped out.Thus, the cost of compares
// is very low relative to the cost of exchanges(move the crates).The warehouse is
// nearly full : there is extra space sufficient to hold any one of the crates, but not two.
// Which sorting method should the clerk use ? Explain your answer.

// The clerk should use selection sort because it is the most efficient and can be in less attempts.
